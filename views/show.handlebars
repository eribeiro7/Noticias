<div class="container mt-9">
    <div class="row">
        <div class="col-sm-12 centered">
            <h4>Dados do Utilizador</h4>
            <div class="media border p-3">
                <img src="../img/users.png" alt="" class="mr-3 mt-3 rounded-circle" style="width:60px;">
                <div class="media-body">
                    <p><h4>{{dados.name}}</h4></p>
                    <p>Tel.: <small><i>{{dados.phone}}</i></small></p>
                    <p>Idade: {{dados.age}}</p>
                    <p>Email: {{dados.email}}</p>
                </div>
            </div>
        </div>
    </div>
</div>